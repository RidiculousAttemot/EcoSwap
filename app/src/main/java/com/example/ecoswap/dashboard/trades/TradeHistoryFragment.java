package com.example.ecoswap.dashboard.trades;

import com.example.ecoswap.dashboard.listings.MyListingsFragment;

/**
 * Legacy entry point preserved for backwards compatibility.
 * Redirects callers to {@link MyListingsFragment} now that trade proofs live inside My Listings.
 */
public class TradeHistoryFragment extends MyListingsFragment {
    // Intentionally empty.
}
